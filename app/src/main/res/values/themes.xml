<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose owns the real theming. This XML theme only covers what the system
        draws before the first frame, so it stays deliberately minimal.
    -->
    <style name="Theme.Idle" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
